We are building a MERN Hotel Management System called LuxuryStay-HMS.

Project structure:
- Main folder has frontend and backend.
- Backend contains package.json, package-lock.json, node_modules, src folder.
- Inside src: app.js and server.js.
- Express is installed and working.
- Server successfully runs with: node src/server.js
- Output: "Server listening on port 5000"

We are proceeding slowly, with small steps (2 at a time max).
Assistant should explain clearly and gently.

.env file exists in backend with PORT=5000.
dotenv is installed.
Current task: update backend/src/server.js to load dotenv before using process.env.

Future requirement (important):
We will use MongoDB Atlas (online), NOT local Compass.
Integration should be taught slowly later.

User prefers reassurance, clarity, and calm pacing.

--------------------------------------------------------------------------------------------

The ONE Thing I Recommend (This Removes 99% of Risk)

Add this small section at the very top of your saved text (or when starting a new chat):

NON-NEGOTIABLE PROJECT PRINCIPLES:
- This must look and feel like a senior-developer, $10k-level product.
- No generic UI libraries or amateur design patterns.
- Clean architecture, step-by-step, explained slowly.
- MongoDB Atlas (online) only, not local Compass.


That’s it.

These act like guard rails.
Even a new assistant cannot drift away from them.


